select u.id,u.name,count(o.id) as order_count from users as u left join orders as o on o.user_id=u.id where u.status='active' and u.deleted_at is null group by u.id,u.name order by order_count desc limit 100;
